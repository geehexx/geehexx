// Minimal offline stub. The project sets RenderCV icons/external-link icons off,
// so fa-icon is only needed to satisfy the package import during compilation.
#let fa-icon(name, size: 1em, ..args) = box(width: 0pt, height: 0pt)
